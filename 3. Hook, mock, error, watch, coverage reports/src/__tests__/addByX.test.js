import { addByX } from "../addByX";

describe('addByX', () => {
  it.todo('should return a func');

  it.todo('should return a func which takes one argument and returns a sum with a value from original func');
})